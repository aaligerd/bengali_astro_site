services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5050:5050"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=5050

    restart: always


